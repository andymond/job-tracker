All Jobs for <strong><%= @company.name %></strong>

<% @company.jobs.each do |job| %>
  <ul><%= job.title %></ul>
<% end %>

<%= render partial: 'companies/contact', collection: @company.contacts.order('lower(name)') %>
<%= render partial: 'contacts/form' %>
